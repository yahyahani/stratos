services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env.local        # ANTHROPIC_API_KEY is read from here at runtime
    restart: unless-stopped
